# Don't re-run an install/status check before `pnpm dev` / `pnpm build`.
# sharp (an optional Next.js image-optimization dep) has a blocked build script,
# which otherwise makes the pre-run check exit non-zero. The app builds without it.
verify-deps-before-run=false
